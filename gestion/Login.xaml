<Page x:Class="gestion.Login"
       xmlns:firebase="clr-namespace:Firebase.Auth.UI;assembly=Firebase.Auth.UI.WPF"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:gestion"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login" FontSize="20" FontFamily="helvetica" Background="#B3E5FC">

    <Page.Resources>
        <!-- Default Text Style -->
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Normal"/>
            <!-- You can change Normal to Bold or another FontWeight -->
            <!-- Add other text properties here if needed -->
        </Style>
    </Page.Resources>
    <Grid >
        <firebase:FirebaseUIControl Background="LightCyan">
            <firebase:FirebaseUIControl.Header>
                <Image Source="/assets/cofat.png" Height="84" RenderTransformOrigin="0.5,0.5">
                   
                </Image>
            </firebase:FirebaseUIControl.Header>
        </firebase:FirebaseUIControl>
        </Grid>
    
</Page>
